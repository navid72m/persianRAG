# Streamlit Community Cloud secrets — paste the same values in the app's
# "Settings -> Secrets" section. Config reads these automatically.
# NEVER commit real keys to a public repo.
OPENCODE_API_KEY = "sk-..."
JINA_API_KEY = "jina_..."
QDRANT_URL = "https://...qdrant.io"
QDRANT_API_KEY = "..."
